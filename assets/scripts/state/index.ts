


export enum Direction {
  idle,
  up,
  upRight,
  right,
  downRight,
  down,
  downLeft,
  left,
  upLeft
}

export enum HeroState {
 idle = 1,
 run = 2,
 swim = 7,
 attack = 4,
 hurt = 11,
 die = 8,
}
